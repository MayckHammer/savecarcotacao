

# Aumentar logo no Header

Aumentar a logo no componente Header que aparece nas telas internas (cotacao, resultado, etc.).

## Alteracao

**Arquivo: `src/components/Header.tsx`**
- Alterar a classe da imagem de `h-12` para `h-20` para deixar a logo maior no header, similar ao tamanho mostrado no print de referencia.

